{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Template list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Id</th>
                <th>Alias</th>
                <th>Subject</th>
                <th>Body</th>
                <th>Active</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('template_show', { 'id': entity.id }) }}">{{ entity.id }}</a></td>
                <td>{{ entity.alias }}</td>
                <td>{{ entity.subject }}</td>
                <td>{{ entity.body }}</td>
                <td>{{ entity.active }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('template_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('template_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('template_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
